const { contactSchema, favoriteSchema } = require('./contactSchema')
const { userSchema, verificationSchema } = require('./userSchema')

module.exports = {
  contactSchema,
  favoriteSchema,
  userSchema,
  verificationSchema
}
